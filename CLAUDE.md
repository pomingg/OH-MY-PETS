# 毛孩生活科技｜企業戰情中心 BI 專案

模擬企業經營資料與戰情中心 Dashboard，作為求職作品集。所有資料皆為虛擬模擬，不代表任何真實企業。

## 交接讀取規則

啟動或被要求「拉齊進度」時，若存在 `.claude/handoff.md`：

1. 先讀完整份 handoff。
2. 動手前，先用自己的話向使用者復述「目標、已定決策、現況、下一步」，等使用者確認。
3. 「已定決策 Decisions」是使用者已拍板的結論，不得擅自更動或推翻；若你認為某項決策有問題，先提出來討論，不要直接改。
4. 「未決事項 Open Questions」是還沒定案的點，動到相關部分前先問使用者，不要自己替他決定。
5. handoff.md 是過渡文件、可能已過期——若內容與實際 repo 狀態衝突，以 repo 現況為準，並把落差指出來。

## 完成後回寫進度

當這一輪實作告一段落（完成了 handoff「下一步」裡的項目、或使用者說做完了）時，主動詢問使用者：「要不要我把這次完成的項目回寫進 `.claude/handoff.md`？」

若使用者同意，更新 handoff：

- 把已完成的項目從「下一步 Next Actions」移到「現況 Current State」的「已完成」。
- 保留還沒做的在「下一步」。
- 其餘欄位維持不變。

目的：讓 handoff 隨時反映「經實作驗證過的最新現況」。這是主動提醒，不是自動執行——使用者沒同意就不要動檔案。

---

## 長期技術決策（不要在未討論的情況下推翻）

以下決策不是顯而易見的，未來容易被誤判為「設計缺陷」而好心改掉，故在此留存理由。

### 資料設計

- **資料生成刻意產出髒資料，再由獨立 ETL 腳本清洗**
  這不是 bug。本專案的目的之一是證明資料清洗能力，若生成階段就產出乾淨資料，清洗環節將無事可做、也就無從證明。生成腳本與清洗腳本必須保持分離，清洗邏輯需有文件記錄。

- **時間彙總不預建彙總表，改在查詢層動態 `GROUP BY`**
  看似效能較差，但換來的是維護成本低與擴充彈性（未來要加週、雙週彙總不需改資料庫結構）。效能問題以索引與 Materialized View 解決，而非預建多份彙總表。

- **`dim_employee` 存 `birth_date` / `hire_date` 而非直接存年齡、年資**
  刻意設計。存日期才能動態計算任一時間點的年齡與年資，支援「趨勢型斷層分析」（例如比較過去兩年斷層是否惡化）；若直接存年齡，只能看死一個時間點的快照。

- **本機資料庫存完整明細，Neon 雲端只放彙總資料**
  Neon 免費方案儲存空間約 0.5GB，每日增量累積的明細會超標。此分層同時對應真實企業「營運資料庫 vs 分析用資料倉儲」概念。

- **每日增量生成必須具備狀態延續性**
  每天不是獨立亂數，必須讀取資料庫現況再往下推進：昨天離職的員工今天不能出現在出勤表、昨日期末庫存即今日期初庫存、進行中訂單需往前推進。破壞這點會讓整份資料失去可追溯性。

### 報表與分析設計

- **資產負債表與現金流量表不隨廠區篩選器變動**
  這不是漏做篩選。單一法人不會依廠區分別編製資產負債表，此為符合會計實務的正確設計，UI 上需保留說明註記。只有損益結構圖會隨廠區篩選。

- **現金流量表採間接法從損益表＋資產負債表推導，不建明細事實表**
  降低跨表邏輯自洽的難度與出錯風險，同時「能用間接法正確推導現金流」本身即是財務分析能力的展示。

- **BCG 矩陣的 Y 軸用「公司內部營收佔比」而非「相對市場佔有率」**
  因為是單一虛擬企業，沒有競爭對手市佔資料。這是在資料限制下的務實調整，非誤用框架，此調整本身是面試講述素材。

### AI 應用

- **Text-to-SQL 採「地端優先、雲端兜底」混合架構**
  地端 Qwen2.5-Coder-14B 處理多數查詢以節省 token；面對 20 餘張表的複雜多表 JOIN 時正確率會下降，故保留 fallback 至 Claude API。這是刻意的成本與品質權衡，不是備援冗餘。

- **Text-to-SQL 需搭配 Schema 精簡策略**
  不要每次把完整 Schema 餵給模型。先判斷問題屬於哪個領域（銷售／生產／人資／財務），只餵相關表結構，可同時降低 token 消耗並提高地端小模型正確率。

- **Text-to-SQL 必須限制只能產出 `SELECT`**
  後端需有檢查層攔截 `DELETE`／`UPDATE`／`DROP`。這是不可省略的安全防護。

- **Schema 描述文件由使用者親自撰寫，不直接丟原始 DDL**
  需包含商業邏輯註解（例如「`fact_orders.actual_ship_date` 為 null 代表尚未出貨」）。這類註解只有懂業務的人寫得出來，直接影響 Text-to-SQL 正確率，也是資料治理能力的展現。

### 前端與視覺

- **Chart.js 等函式庫必須內嵌於 HTML，不使用外部 CDN**
  已實測踩過此坑：部分瀏覽器沙盒／CSP 環境會封鎖外部網域請求，導致 `Chart is not defined`、圖表全部無法顯示。內嵌後離線可用，更穩健。

- **圖表區塊採整頁大塊嵌入 iframe，不零散拆分**
  每個分頁的所有圖表放在同一個 iframe 內。零散嵌入會有邊界、間距難控制、效能差；大塊嵌入才能讓版面配置與卡片對齊完全由自訂 CSS 掌控。

- **視覺精緻度為硬性需求，不得使用 Streamlit 預設樣式呈現主要內容**
  使用者對介面與數據視覺化精緻度要求很高。必須注入全域 CSS 覆寫 Streamlit 外殼、隱藏預設 UI 元素（`#MainMenu`、`footer`、預設 header）、改造原生篩選器外觀。開發完成後需與 `戰情中心原型.html` 截圖比對確認一致性。

- **設計 token（不得任意更動）**
  暗色系指揮中心風格。四領域專屬色：銷售 `#E8A33D`、生產 `#35B0AE`、人資 `#9B84E8`、財務 `#4FBD84`。字體：Space Grotesk（標題）／Noto Sans TC（內文）／IBM Plex Mono（數字）。
