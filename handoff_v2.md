# 毛孩生活科技｜企業戰情中心 BI 作品集專案

> 交接時間：2026-08-20 ｜來源：Claude.ai 討論（v2，取代先前版本）

## 目標 Objective
建立一個模擬製造業（寵物用品）的完整企業經營資料與戰情中心，作為求職作品集。需具體證明四項能力：**Tableau 實務使用經驗**、**資料清洗能力**、**跨部門數據規劃與分析能力**、**將 LLM 落地整合進 BI 工作流的能力**（因真實工作資料涉及公司機密無法直接使用）。

最終產出物：
1. 自建 PostgreSQL 資料庫（本機建置 → Neon 雲端展示）
2. 可重現的資料生成腳本（含刻意產生的髒資料）＋ 獨立的 ETL 清洗腳本
3. 每個上班日自動增量生成資料的排程系統（模擬公司真實日常營運）
4. Streamlit 主應用（嵌入高精緻度自訂 HTML 圖表）＋ AI 功能整合
5. Tableau Public 作品（獨立平行呈現，作為 BI 工具經驗佐證）
6. 案例說明文件（商業假設、清洗方法論、AI 協作方法論、面試講述稿）

## 已定決策 Decisions

### 企業設定與資料模型
- 決策：虛擬企業設定為「寵物用品製造業」（自有品牌成品＋零售通路代工兩條產品線）｜理由：原提案為汽機車製造業，後改為寵物用品，因產品複雜度更單純、通路故事更豐富（電商DTC＋實體經銷＋代工客戶），且資料表已採通用命名設計，改產業不需重做架構。
- 決策：企業規模為中型，年營收約 60-80 億，3 座廠區（A廠主力組裝／B廠零組件／C廠衛星廠），約 10-14 個產品料號｜理由：規模需足以支撐分產品線／分廠區下鑽分析，但不宜過度複雜。
- 決策：戰情中心四大領域（財務、銷售、生產與庫存、人力資源）份量對等｜理由：使用者現職負責多領域綜合戰情室，需展現同等分析深度。
- 決策：現金流量表採「間接法」從損益表＋資產負債表月度變動推算，不另建明細事實表｜理由：降低跨表邏輯出錯風險，且「用間接法正確推導現金流」本身就是財務分析能力展示。
- 決策：資產負債表與現金流量表為法人整體層級指標，UI 上不隨廠區篩選器變動，僅損益結構圖隨廠區篩選｜理由：單一法人不會依廠區分別編製資產負債表，符合會計實務，UI 需加註說明。
- 決策：報表的時間彙總（月／季／年）不預先在資料庫存彙總表，改在查詢層動態 `GROUP BY`｜理由：降低維護成本，保留未來擴充彈性（週、雙週彙總）。

### 資料生成與清洗（重要：這是後補的關鍵決策）
- 決策：資料生成分兩階段——**階段一為一次性歷史回填**（2023/01 至今，建立3年歷史基礎）；**階段二為每日增量生成**（從今日起，每個上班日只生成當日一天份資料）｜理由：需要足夠歷史跨度做年增率／季節性分析，同時要有「活的」持續更新資料模擬真實企業日常營運。注意：不是每天重複生成3年份資料。
- 決策：資料生成腳本必須先產出**刻意帶有瑕疵的原始資料**（重複記錄、缺失值、日期格式混用、全半形不一致、型別錯誤、離群值、跨表對不上的外鍵），再由**獨立的 ETL 清洗腳本**處理成乾淨資料後才匯入正式資料庫｜理由：使用者需證明「資料清洗能力」，但純粹用程式生成的乾淨資料無法證明此項能力——沒有髒資料就沒有清洗可言。清洗邏輯與判斷依據需有文件記錄，並產出清洗前後資料品質對比報告。
- 決策：每日增量生成必須具備「狀態延續性」——讀取資料庫現況再往下推進，而非每次獨立亂數｜理由：昨天離職的員工今天不能出現在出勤表、昨天期末庫存必須是今天期初庫存、進行中訂單需往前推進，否則資料失去真實性與可追溯性。
- 決策：每日生成需搭配營業日曆（含國定假日、週末、春節連假、寵物用品業旺季備貨期）與事件機率模型（離職、設備故障、供應商延遲以機率分布觸發，如每月離職率約1.2%）｜理由：寫死排程的事件不具真實感，機率模型才能產生自然的營運波動。
- 決策：排程由作業系統執行（Windows 工作排程器／cron），Claude 的角色是撰寫腳本而非每日手動執行｜理由：無人值守、零 token 消耗、可版控，且展現完整自動化工程能力。腳本需具備補跑機制（若停機數日，能一次補齊未生成的日期，不可斷鏈）。
- 決策：每日增量生成排定於**每個上班日下午 5:00** 執行｜理由：對應一般企業下班時點，該日營運資料已完整，符合真實企業日結作業節奏。

### 技術選型
- 決策：主應用採 **Streamlit 為外殼，嵌入自訂 HTML 圖表區塊**（做法A）｜理由：核心目標是證明 BI 與分析能力而非前端工程；AI 功能、預測模型、資料庫連線全在 Python 生態系，Streamlit 開發效率最高；同時透過嵌入 HTML 保有視覺自由度，維持單一應用、單一網址的完整體驗，避免兩套系統造成體驗割裂。
- 決策：資料庫採本機 PostgreSQL 建置練習，展示時遷移至 Neon 雲端｜理由：PostgreSQL 履歷含金量高於 SQLite，可學習資料庫服務架設、Schema 設計、連線管理；Neon 有永久免費方案適合長期公開展示。
- 決策：**本機資料庫存完整明細，推上 Neon 的是彙總後的展示用資料集**｜理由：Neon 免費方案儲存空間約 0.5GB，每日累積明細資料會超標；此分層也對應真實企業「營運資料庫 vs 分析用資料倉儲」概念，是加分點。
- 決策：Tableau 定位為**輔助佐證**而非主工作流——用 Tableau Desktop 連本機 PostgreSQL 開發戰情室，擷取成 Extract 後發布至 Tableau Public｜理由：使用者需證明 Tableau 實務經驗，但主要工作流已定為 Streamlit。注意：Tableau Public 免費版無法直連資料庫，故 Tableau 版本會是靜態快照、不隨每日資料更新，此為已知限制。
- 決策：Tableau 版本需刻意展示進階技巧（LOD 運算式、Parameters、計算欄位、Dashboard Actions 點擊聯動、Sets/Groups）｜理由：基本拖拉圖表無法證明「豐富經驗」，需要進階功能才有說服力。
- 決策：Tableau 版本不複刻全部內容，四大領域**各挑選數個重點指標**呈現即可｜理由：Tableau 定位為 BI 工具經驗佐證，非主工作流；重點在展示進階技巧的運用深度，而非畫面數量的廣度。挑選原則建議以「最能展現差異化分析視角」的指標優先（如銷售的 BCG 矩陣、人資的斷層分析）。
- 決策：Chart.js 等前端函式庫需內嵌於 HTML，不透過外部 CDN 載入｜理由：實測發現部分瀏覽器沙盒／CSP 環境會封鎖外部網域請求，導致 `Chart is not defined`、圖表全部無法顯示。

### AI 應用架構（層次一至三完整投入）
- 決策：AI 應用分三層全部實作｜理由：使用者求職目標包含 AI 應用方向，需完整展現 LLM 落地能力。
  - 層次一：AI 協作方法論文件化（記錄如何用 AI 協作設計資料模型與 KPI）＋ 統計式自動異常偵測（Z-score／移動平均標準差，取代寫死的警示門檻）
  - 層次二：預測分析模組（營收預測用 Prophet；離職風險預測用 scikit-learn 分類模型，呼應已設計的人才斷層分析；經銷商信用風險評分）
  - 層次三：Text-to-SQL 自然語言查詢介面 ＋ AI 自動生成經營週報
- 決策：地端 LLM 架設於使用者本機（RTX 5060 Ti 16GB VRAM），選用 **Qwen2.5-Coder-14B 量化版**｜理由：16GB VRAM 可順跑 14B 等級量化模型；Coder 版本的 SQL 生成能力在此尺寸級距最強；地端執行可大幅節省 token 消耗。
- 決策：採「**地端優先、雲端兜底**」混合架構——經營週報生成與簡單摘要走地端；Text-to-SQL 優先地端，失敗或複雜多表 JOIN 時 fallback 至 Claude API｜理由：地端小模型處理敘述性生成完全勝任，但面對 20 餘張表的複雜 JOIN 正確率會下降；此設計本身是「成本與品質權衡」的工程實踐案例，面試可講述。
- 決策：Text-to-SQL 需搭配「**Schema 精簡策略**」——先判斷問題屬於哪個領域（銷售／生產／人資／財務），只餵入相關表結構而非完整 Schema｜理由：降低 token 消耗與模型負擔，同時提高地端小模型正確率。
- 決策：Text-to-SQL 必須有安全性防護，限制只能產出 `SELECT`，後端需加檢查層攔截 `DELETE`／`UPDATE`／`DROP`｜理由：資安基本要求，亦是面試可展示的細節。
- 決策：Schema 描述文件需由使用者親自撰寫（非直接丟原始 DDL 給 LLM），需包含商業邏輯註解（例如「fact_orders 的 actual_ship_date 為 null 代表尚未出貨」）｜理由：這類註解只有懂業務的人寫得出來，本身即是資料治理能力的展現，也直接影響 Text-to-SQL 正確率。
- 決策：經營週報生成採「異常優先」邏輯——先將自動異常偵測結果餵給 LLM，讓它優先描述異常項目｜理由：避免流水帳式念數字，聚焦真正有變化的指標。

### 視覺精緻度規格（使用者明確表示高要求，不得妥協）
- 決策：視覺必須對照 `戰情中心原型.html` 的設計 token（暗色系指揮中心風格；四領域專屬色：銷售＝琥珀金 #E8A33D、生產＝青綠 #35B0AE、人資＝紫 #9B84E8、財務＝綠 #4FBD84；字體 Space Grotesk／Noto Sans TC／IBM Plex Mono）實作，**不得使用 Streamlit 預設樣式呈現主要內容**｜理由：使用者對介面與數據視覺化精緻度要求很高，這是硬性需求。
- 決策：必須注入全域 CSS 覆寫 Streamlit 外殼樣式，隱藏預設 UI 元素（`#MainMenu`、`footer`、預設 header），並改造原生篩選器元件外觀｜理由：避免「精緻圖表配陽春外框」的視覺違和。
- 決策：圖表區塊採「**整頁大塊嵌入**」——每個分頁的所有圖表在同一個 iframe 內，而非每張圖各自嵌入小 iframe｜理由：零散嵌入會有邊界、間距難控制、效能差；大塊嵌入可讓版面配置與卡片對齊完全由自訂 CSS 掌控。
- 決策：Streamlit 篩選器與 HTML 圖表的連動，採「Python 動態注入資料至 HTML template 後重新渲染」方式處理｜理由：`st.components.v1.html()` 跑在 iframe 中無法直接讀取 Streamlit 狀態，需此變通做法。iframe 高度需明確指定，設計時就固定各區塊高度規格。
- 決策：每次視覺相關開發完成後，需與原型截圖比對確認一致性｜理由：確保開發過程不會逐步妥協視覺品質。

### 其他
- 決策：需加入全域時間軸選擇器（月／季／年粒度切換＋自訂範圍＋自動比較基準 MoM/QoQ/YoY），置於頂部，統一控制所有分頁｜理由：戰情室需維持「同一視角看全公司」的一致性才能做跨領域關聯分析。
- 決策：需設計資料庫索引與 Materialized View 優化查詢效能｜理由：每日累積明細資料會持續成長，Dashboard 載入會變慢；此優化亦是多數 BI 履歷缺少的深度。
- 決策：案例說明文件需誠實聲明「本專案資料視覺化邏輯參考本人於現職使用 Tableau 建置戰情中心之經驗，以網頁技術重製呈現」｜理由：誠實交代來源，同時展現「不只會用工具，也能自己刻」。

## 現況 Current State
- 已完成：
  - 企業設定、資料表結構（維度表＋事實表）已全數定案
  - 四大領域 KPI 清單（標準＋差異化）已全數定案
  - 互動式 Dashboard 視覺原型完成（`戰情中心原型.html`），含總覽頁＋四大領域分頁、Chart.js 圖表、各分頁篩選器示範
  - 原型已修正 Chart.js 外部 CDN 載入失敗問題（改內嵌函式庫）
  - 技術選型、AI 應用架構、資料清洗策略、每日自動生成機制、視覺規格均已討論定案
- 進行中：無（規劃階段完成，尚未動工）
- 尚未開始：所有實作項目（見下方 Next Actions）

## 下一步 Next Actions
1. 協助使用者在本機安裝 PostgreSQL 並建立資料庫服務（使用者希望學習此過程，請一步步引導並記錄安裝過程供日後寫入案例文件）
2. 依資料字典建立完整 Schema 與所有維度表／事實表的 DDL
3. 撰寫資料生成腳本（階段一：一次性回填 2023/01 至今的歷史資料），需刻意注入資料瑕疵（重複、缺失、格式不一致、型別錯誤、離群值、外鍵對不上），並埋入 3-4 個異常事件軸（供應商斷料、設備故障、旺季波動），確保跨表邏輯自洽
4. 撰寫獨立 ETL 清洗腳本，處理上述髒資料，記錄清洗邏輯與判斷依據，產出清洗前後資料品質對比報告
5. 將清洗後資料匯入 PostgreSQL，建立索引與必要的 Materialized View
6. 撰寫每日增量生成腳本（階段二），需具備狀態延續性、營業日曆、事件機率模型、補跑機制；設定作業系統排程自動執行
7. 由使用者親自撰寫 Schema 描述文件（含商業邏輯註解），供 Text-to-SQL 使用
8. 建置 Streamlit 主應用骨架：全域 CSS 覆寫、隱藏預設 UI、頂部品牌列與分頁籤、全域時間軸選擇器
9. 開發各分頁 HTML 圖表區塊（整頁大塊嵌入），對照 `戰情中心原型.html` 視覺規格，串接 PostgreSQL 實際資料
10. 協助在本機架設地端 LLM（Ollama 或 LM Studio ＋ Qwen2.5-Coder-14B 量化版），測試效能並與應用串接
11. 開發 AI 層次一：統計式自動異常偵測（Z-score／移動平均標準差）
12. 開發 AI 層次二：預測模組（營收預測 Prophet、離職風險預測 scikit-learn、經銷商信用風險評分）
13. 開發 AI 層次三：Text-to-SQL 介面（含 Schema 精簡策略、SELECT-only 安全檢查、地端優先／Claude API fallback）與 AI 經營週報生成（異常優先邏輯）
14. 用 Tableau Desktop 連本機 PostgreSQL 開發戰情室（刻意運用 LOD、Parameters、Dashboard Actions 等進階技巧），擷取 Extract 後發布至 Tableau Public
15. 將彙總後展示用資料集遷移至 Neon 雲端，部署 Streamlit 應用供公開展示
16. 撰寫案例說明文件（商業假設、資料建模方法論、清洗方法論、AI 協作方法論、Tableau 技巧亮點、面試講述稿、Tableau 經驗轉譯聲明）

## 技術脈絡 Context

**Stack / 環境**
- 資料庫：PostgreSQL（本機完整明細）→ Neon（雲端彙總展示）
- 資料處理：Python（生成、ETL 清洗、統計異常偵測）
- 預測分析：Prophet、scikit-learn
- 地端 LLM：Ollama / LM Studio ＋ Qwen2.5-Coder-14B 量化版（RTX 5060 Ti 16GB VRAM）
- 雲端 LLM：Claude API（複雜查詢 fallback）
- 主應用：Streamlit ＋ 嵌入自訂 HTML/CSS/JS（Chart.js 需內嵌，勿用 CDN）
- BI 工具佐證：Tableau Desktop → Tableau Public
- 排程：Windows 工作排程器 / cron

**關鍵檔案**
- `戰情中心原型.html`：視覺與互動原型，含完整 CSS 設計 token、四頁圖表配置、篩選器邏輯。**這是正式開發的視覺規格基準，開發時必須對照實作**

**資料字典（核心表結構）**
- 維度表：
  - `dim_date`（含旺季/淡季/國定假日標記）
  - `dim_plant`（A廠主力組裝／B廠零組件／C廠衛星廠）
  - `dim_product`（2條產品線，約10-14料號，含 unit_cost、suggested_price、launch_date、main_material）
  - `dim_channel`（電商DTC／實體經銷／零售代工）
  - `dim_dealer`（含 cooperation_start_date、payment_terms_days）
  - `dim_supplier`、`dim_department`、`dim_employee_type`
  - `dim_employee`（員工主檔，含 birth_date、hire_date、job_grade；刻意存日期而非年齡/年資，以支援任一時間點的動態計算與趨勢型斷層分析）
- 事實表：
  - `fact_orders`（含 promised_ship_date、actual_ship_date、payment_status）
  - `fact_sales_return`（含 return_reason，為銷售與品質的連結點）
  - `fact_production`（日產出、良率、稼動、downtime_reason）
  - `fact_inventory`（庫存水位、進出貨、days_in_stock）
  - `fact_supplier_delivery`（交貨、品檢合格量）
  - `fact_hr_headcount`、`fact_hr_attendance`、`fact_hr_cost`、`fact_safety`
  - `fact_finance`（月度損益，含 budget_revenue）
  - `fact_ar_ap`（應收應付、帳齡）
  - `fact_balance_sheet`（月度資產負債快照）

**四大領域 KPI 目錄**
- 銷售（標準）：營收達成率、YoY、分產品線業績佔比、訂單達交率、平均交期、經銷商業績成長率、客戶集中度、回購率
- 銷售（差異化）：BCG矩陣（公司內部貢獻版，X軸＝年增率／Y軸＝業績佔比，取代傳統市佔率——因單一虛擬企業無競爭對手市佔資料，此為務實調整，面試可講述）、退貨率（分產品／分原因）、新品活力指數、業績佔比vs毛利佔比落差分析、通路-產品適配度、動銷率
- 生產與庫存：稼動率、良率、OEE、庫存周轉天數、呆滯庫存比例、供應商準時交貨率、停機原因分布（全數採標準KPI）
- 人力資源（標準）：離職率、出勤率、人均產值、人力成本佔營收比、工安事件率
- 人力資源（差異化）：年齡／年資／職等結構分布斷層分析、斷層量化判斷規則（任一區間人數低於相鄰區間均值40%標記警訊）、接班風險交叉分析（職等×年齡）
- 財務（標準）：毛利率、營業利益率、淨利率、DSO/DPO、ROA
- 財務（差異化）：資產負債表健康度指標（流動比率、負債比率）、間接法推導現金流量表

**篩選器邏輯（已於原型示範）**
- 銷售頁：產品線＋通路（連動 BCG矩陣、退貨率、營收趨勢、通路佔比）
- 生產頁：廠區（連動 OEE趨勢、停機原因、呆滯庫存、供應商排行）
- 人資頁：部門（連動年齡/年資/職等三圖，並反白接班風險表對應列）
- 財務頁：廠區（僅影響損益結構圖）

**限制條件**
- 不得使用瀏覽器 localStorage / sessionStorage
- 外部函式庫需內嵌，不用 CDN
- Neon 免費方案儲存約 0.5GB，雲端僅存彙總資料
- Tableau Public 無法直連資料庫，僅能發布靜態 Extract 快照
- iframe 高度需手動指定，無法自動適應內容

## 邊界 Out of Scope
- 真實企業資料一律不得使用，所有資料皆為虛擬模擬，不代表任何真實企業經營狀況
- 本階段不處理使用者登入／權限系統
- 不做行動裝置原生 App，僅網頁響應式設計
- Tableau 版本不需要做到與 Streamlit 版本功能對等，定位為 BI 工具經驗佐證

## 未決事項 Open Questions
- 全域時間軸選擇器僅完成需求討論，尚未實際示範互動效果；實作時「自訂範圍」的具體 UI 元件型式需與使用者確認
- 是否要在正式版加入資料下鑽（drill-down）點擊互動，尚未討論
- 案例說明文件的完整格式與撰寫時機尚未討論
- Tableau 各領域具體要挑哪幾個指標，待實作時與使用者確認（已定「各挑數個重點」原則，但未逐項指定）
